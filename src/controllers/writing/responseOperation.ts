// parse response from API
/** should be an single function store in "../../utils"
 *  and export this single function in "writingService.ts"
 */

// Change the file name to writingResponse

/**
 * Replace the content of this template to the actual comments
 * Returns x raised to the n-th power.
 * @param {number} x The number to raise.
 * @param {number} n The power, must be a natural number.
 * @return {number} x raised to the n-th power.
 * if no return, you don't have to add this @return value in comments
 * @source url
 */

// const responseOperation = () => {};

// export { responseOperation };
